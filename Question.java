package miniproject;
//create a class to represent questions for quiz application
public class Question {
	String prompt;//prompt for the question
	String answer;
	//question has two attributes that stores inside the class
	public Question (String prompt,String answer)//we accept two paramaters
	{
		this.prompt=prompt;
		this.answer=answer;
	}

	
}
